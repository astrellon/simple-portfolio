{
  "name": "simple-cms",
  "version": "1.0.0",
  "description": "A simple CMS",
  "main": "index.js",
  "scripts": {
    "start": "ts-node src/index.ts --jsx",
    "buildClient": "rm -rf ./clientDeploy/ && cd ../client && npm run build && cp -r ./deploy ../server/clientDeploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alan Lawrey",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.14.31",
    "array-flat-polyfill": "^1.0.1",
    "node-html-parser": "^3.0.3",
    "simple-data-store": "^3.3.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.5"
  },
  "dependencies": {
    "simple-tsx-vdom": "file:../../simple-vdom"
  }
}
